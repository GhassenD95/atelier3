{% extends 'base.html.twig' %}

{% block title %}Hello AuthorController!{% endblock %}

{% block body %}
    <div class="flex flex-col items-center">
        <h1 class="text-xl">Hello</h1>
        {% if name is not null %}
        <p class="text-3xl">{{ name }}</p>
        {% else %}
        <p class="text-3xl">stranger</p>
        {% endif %}
    </div>
{% endblock %}
